name: Deploy batch to AWS EC2

on:
  push:
    branches:
      - batch  # batch 브랜치 푸시 시 실행

jobs:
  build-and-push-batch:
    name: Build and Push batch Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
            distribution: 'temurin'
            java-version: '21'
      
      - name: Fix Code Formatting
        run: ./gradlew spotlessApply

      - name: Set Environment Variables
        run: echo "SENTRY_AUTH_TOKEN=${{ secrets.SENTRY_AUTH_TOKEN }}" >> $GITHUB_ENV

      - name: Build Spring Boot JAR
        env:
            SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }} 
        run: |
          chmod +x gradlew
          ./gradlew clean build -x test

      - name: Configure AWS Credentials
        run: |
            aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
            aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            aws configure set region ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        run: |
            aws ecr get-login-password --region ${{ secrets.AWS_REGION }} | docker login --username AWS --password-stdin ${{ secrets.ECR_REGISTRY }}

      - name: Set Version Tag
        run: echo "VERSION_TAG=$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_ENV

      - name: Build batch Docker Image
        run: |
            docker build -t ${{ secrets.ECR_REGISTRY }}/siungongsi:latest-batch .
            docker tag ${{ secrets.ECR_REGISTRY }}/siungongsi:latest-batch \
            ${{ secrets.ECR_REGISTRY }}/siungongsi:${{ env.VERSION_TAG }}-batch

      - name: Push batch Docker Image
        run: |
            docker push ${{ secrets.ECR_REGISTRY }}/siungongsi:latest-batch
            docker push ${{ secrets.ECR_REGISTRY }}/siungongsi:${{ env.VERSION_TAG }}-batch

  deploy:
    name: Deploy to EC2
    needs: [build-and-push-batch]
    runs-on: ubuntu-latest

    steps:
      - name: SSH into EC2 and Deploy
        uses: appleboy/ssh-action@v0.1.10
        with:
            host: ${{ secrets.BATCH_EC2_HOST }}
            username: ubuntu
            key: ${{ secrets.EC2_SSH_KEY }}
            script: |
                echo "Pulling latest images..."
                aws ecr get-login-password --region ${{ secrets.AWS_REGION }} | docker login --username AWS --password-stdin ${{ secrets.ECR_REGISTRY }}

                docker pull ${{ secrets.ECR_REGISTRY }}/siungongsi:latest-batch

                echo "Stopping and removing old containers..."
                docker stop siungongsi-batch || true
                docker rm siungongsi-batch || true

                echo "Running new batch container..."
                docker run -d --name siungongsi-batch -p 8080:8080 \
                    --restart always \
                    -e TZ=Asia/Seoul \
                    -e DB_HOST=${{ secrets.DB_HOST }} \
                    -e DB_PORT=${{ secrets.DB_PORT }} \
                    -e DB_USER=${{ secrets.DB_USER }} \
                    -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} \
                    -e DB_DATABASE=${{ secrets.DB_DATABASE }} \
                    -e AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }} \
                    -e AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }} \
                    -e AWS_REGION=${{ secrets.AWS_REGION }} \
                    -e SENTRY_AUTH_TOKEN=${{ secrets.SENTRY_AUTH_TOKEN }} \
                    -e SENTRY_DSN=${{ secrets.SENTRY_DSN }} \
                    -e OPENDART_API_KEY=${{ secrets.OPENDART_API_KEY }} \
                    -e GOOGLE_AI_API_KEY=${{ secrets.GOOGLE_AI_API_KEY }} \
                    -e SPRING_DATASOURCE_URL=${{ secrets.SPRING_DATASOURCE_URL }} \
                    -e SPRING_DATASOURCE_USERNAME=${{ secrets.SPRING_DATASOURCE_USERNAME }} \
                    -e SPRING_DATASOURCE_PASSWORD=${{ secrets.SPRING_DATASOURCE_PASSWORD }} \
                    -e AWS_BUCKET_NAME=${{ secrets.AWS_BUCKET_NAME }} \
                    -e AWS_SQS_QUEUE_NAME=${{ secrets.AWS_SQS_QUEUE_NAME }} \
                    -e AWS_SQS_QUEUE_URL=${{ secrets.AWS_SQS_QUEUE_URL }} \
                    -e KOREAN_INVESTMENT_APPKEY=${{ secrets.KOREAN_INVESTMENT_APPKEY }} \
                    -e KOREAN_INVESTMENT_SECRETKEY=${{ secrets.KOREAN_INVESTMENT_SECRETKEY }} \
                    -e ADMIN_KEY=${{ secrets.ADMIN_KEY }} \
                    -e JWT_SECRET_KEY=${{ secrets.JWT_SECRET_KEY }} \
                    -e JWT_EXPIRATION_TIME=${{ secrets.JWT_EXPIRATION_TIME }} \
                    -e FCM_KEY=${{ secrets.FCM_KEY }} \
                    -e SITE_URL=${{ secrets.SITE_URL }} \
                    ${{ secrets.ECR_REGISTRY }}/siungongsi:latest-batch
